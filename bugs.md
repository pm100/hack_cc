# Outstanding issues

No known outstanding issues are currently tracked from the previous `bugs.md` list.

Resolved in this pass:

1. File-scope visibility and local `extern`/linkage checks.
2. Conflicting `signed`/`unsigned` redeclarations.
3. Standalone incomplete struct forward declarations (`struct Name;`).
4. Member and array access on returned struct temporaries.
5. Struct parameter/return regressions exposed by the chapter 18 external suite.
